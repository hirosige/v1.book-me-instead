const { countries } = require('../data/area.json')
const { advantages } = require('../data/advantages.json')
const { postCategories } = require('../data/postCategory.json')

module.exports = {
  countries,
  advantages,
  postCategories
}